# Copy this file to .env and configure your settings
# Generate a secret key with: python3 -c "import secrets; print(secrets.token_hex(32))"

# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key
FLASK_ENV=development

# Database Configuration
# For local development (SQLite - no setup required):
DATABASE_URL=sqlite:///meeting_minutes.db

# For production (PostgreSQL):
# DATABASE_URL=postgresql://username:password@localhost:5432/meeting_minutes

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Optional: Reverse proxy settings (if using nginx in production)
# PROXY_FIX=true
